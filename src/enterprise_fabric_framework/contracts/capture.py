"""Versioned bounded contract for a completed source-to-Bronze capture.

The concrete model will contain run identities, source boundaries and relation/file references,
never business rows.
"""

__all__: tuple[str, ...] = ()
